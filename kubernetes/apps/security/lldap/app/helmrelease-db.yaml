---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: lldap-postgresql
  namespace: security
spec:
  interval: 30m
  chart:
    spec:
      chart: postgresql
      version: 13.2.5
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    auth:
      existingSecret: lldap-secret
      secretKeys:
        userPasswordKey: INIT_POSTGRES_PASS
      username: ${LLDAP_DB_USER}
      database: ${LLDAP_DB_NAME}
    primary:
      persistence:
        enabled: true
        existingClaim: lldap-postgresql-pvc
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
