---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: authelia-postgresql
  namespace: security
spec:
  interval: 30m
  chart:
    spec:
      chart: postgresql
      version: 13.2.5
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    auth:
      existingSecret: authelia-secret
      secretKeys:
        userPasswordKey: AUTHELIA_STORAGE_POSTGRES_PASSWORD
        adminPasswordKey: POSTGRES_SUPER_PASS
      username: ${AUTHELIA_DB_USER}
      database: ${AUTHELIA_DB_NAME}
    primary:
      persistence:
        enabled: true
        existingClaim: authelia-postgresql-pvc
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
